# ============================================================
# Grafana Datasource — Prometheus
# Phase 4 | Auto-provisioned at Grafana startup
# ============================================================

apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
